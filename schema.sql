CREATE TABLE employees(
	emp_no INT PRIMARY KEY NOT NULL,
	emp_title_id VARCHAR(10),
	birth_date DATE,
	first_name VARCHAR,
	last_name VARCHAR,
	SEX VARCHAR,
	hire_date DATE);
	
SELECT * FROM employees;
